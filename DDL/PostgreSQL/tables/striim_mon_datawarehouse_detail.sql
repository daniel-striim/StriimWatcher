CREATE TABLE mon.striim_mon_datawarehouse_detail (
    dwdid BIGINT PRIMARY KEY,
    batchdate TIMESTAMP,
    appName TEXT,
    sourceName TEXT,
    targetName TEXT,
    target_adaptername TEXT,
    projectId TEXT,
    Mode TEXT,
    streamingUpload BOOLEAN,
    StreamingConfiguration TEXT,
    optimizedMerge BOOLEAN,
    batch_event_count INTEGER,
    batch_interval INTEGER,
    total_batches_created INTEGER,
    partition_pruned_batches INTEGER,
    last_successful_merge_time TIMESTAMP,
    total_batches_ignored INTEGER,
    max_integration_time_ms INTEGER,
    avg_in_mem_compaction_time_ms NUMERIC,
    avg_batch_size_bytes BIGINT,
    avg_event_count_per_batch NUMERIC,
    min_integration_time_ms INTEGER,
    total_batches_queued INTEGER,
    avg_compaction_time_ms NUMERIC,
    avg_waiting_time_in_queue_ms NUMERIC,
    avg_integration_time_ms NUMERIC,
    total_batches_uploaded INTEGER,
    avg_merge_time_ms NUMERIC,
    avg_stage_resources_mgmt_time_ms NUMERIC,
    avg_upload_time_ms NUMERIC,
    last_batch_updates INTEGER,
    last_batch_event_count INTEGER,
    last_batch_inserts INTEGER,
    last_batch_max_record_size_bytes BIGINT,
    last_batch_total_events_merged INTEGER,
    last_batch_ddls INTEGER,
    last_batch_sequence_number INTEGER,
    last_batch_size_bytes BIGINT,
    last_batch_deletes INTEGER,
    last_batch_pk_updates INTEGER,
    last_batch_accumulation_time_ms INTEGER,
    last_batch_compaction_time_ms INTEGER,
    last_batch_stage_resources_mgmt_time_ms INTEGER,
    last_batch_upload_time_ms INTEGER,
    last_batch_merge_time_ms INTEGER,
    last_batch_in_mem_compaction_time_ms INTEGER,
    last_batch_pk_update_time_ms INTEGER,
    last_batch_ddl_execution_time_ms INTEGER,
    last_batch_total_integration_time_ms INTEGER,
    targetComponentName TEXT
);
